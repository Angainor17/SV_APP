<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Нейтральная тема без DayNight - вся тема управляется через Compose SVAPPTheme -->
    <!-- MIUI не корректно обрабатывает AppCompatDelegate.setDefaultNightMode() -->
    <!-- Поэтому используем нейтральную тему и управляем цветами только из Compose -->
    <!-- Splash Screen атрибуты перенесены в values-v31/themes.xml для API 31+ -->
    <style name="Theme.SVAPP" parent="Theme.AppCompat.NoActionBar">
        <!-- Материал3 цвета будут установлены через Compose SVAPPTheme -->
    </style>
</resources>